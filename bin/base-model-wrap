#!/usr/bin/env node
//#region @backend
// console.log('-- FIREDEV started... please wait. --')
// require('cache-require-paths');
var path = require('path');
var bundle = require(path.join(__dirname, '../bundle/index.js'));
var start = bundle.default;
console.log(bundle.helo)
start(process.argv);
